package org.dccio.core;

/**
 * Defines the role(s) a controller can play in the system.
 */
public enum ControllerRole {
    THROTTLES,      // Controller handles throttle operations
    ACCESSORIES     // Controller handles accessory (turnout) operations
}

